<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Certificate of Appreciation</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Montserrat:wght@300;400;500&family=Dancing+Script:wght@700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Montserrat', sans-serif;
      margin: 0;
      background: #f5f5f5;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background-image: radial-gradient(#e0e0e0 1px, transparent 1px);
      background-size: 20px 20px;
    }

    .certificate-container {
      width: 800px;
      height: 600px;
      padding: 40px;
      text-align: center;
      border: 15px solid #0a2351;
      border-radius: 15px;
      position: relative;
      margin: 20px auto;
      background: #fff;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
      overflow: hidden;
    }

    /* Watermark */
    .certificate-container::before {
      content: "RID BHARAT";
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%) rotate(-45deg);
      font-size: 80px;
      color: rgba(10, 35, 81, 0.05);
      font-family: 'Playfair Display', serif;
      font-weight: 700;
      z-index: 0;
      letter-spacing: 10px;
    }

    .certificate-header {
      margin-top: 30px;
      position: relative;
      z-index: 2;
    }

    .certificate-title {
      font-family: 'Dancing Script', cursive;
      font-size: 72px;
      color: #0a2351;
      margin: 0;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
      letter-spacing: 3px;
    }

    .certificate-subtitle {
      font-size: 28px;
      color: #c9a145;
      margin: 10px 0 40px;
      font-weight: 500;
      letter-spacing: 8px;
      text-transform: uppercase;
    }

    .logo-top-left {
      position: absolute;
      top: 30px;
      left: 30px;
      width: 180px;
      height: auto;
      z-index: 2;
    }

    .certificate-content {
      font-size: 20px;
      color: #333;
      margin: 40px 80px;
      line-height: 2;
      position: relative;
      z-index: 2;
    }

    .certificate-content p {
      margin: 25px 0;
    }

    .certificate-content strong {
      color: #0a2351;
      font-weight: 500;
      font-size: 24px;
    }

    .footer {
      position: absolute;
      bottom: 50px;
      left: 0;
      width: 100%;
      text-align: center;
      z-index: 2;
    }

    .signature {
      margin: 15px auto;
      width: 250px;
      border-top: 2px solid #0a2351;
      padding-top: 5px;
      font-style: italic;
    }

    .date {
      margin-top: 15px;
      color: #0a2351;
      font-size: 18px;
      font-weight: 500;
    }

    .corner-decoration {
      position: absolute;
      width: 60px;
      height: 60px;
      z-index: 1;
    }

    .top-left {
      top: 25px;
      left: 25px;
      border-top: 4px solid #c9a145;
      border-left: 4px solid #c9a145;
    }

    .top-right {
      top: 25px;
      right: 25px;
      border-top: 4px solid #c9a145;
      border-right: 4px solid #c9a145;
    }

    .bottom-left {
      bottom: 25px;
      left: 25px;
      border-bottom: 4px solid #c9a145;
      border-left: 4px solid #c9a145;
    }

    .bottom-right {
      bottom: 25px;
      right: 25px;
      border-bottom: 4px solid #c9a145;
      border-right: 4px solid #c9a145;
    }

    .seal {
      position: absolute;
      bottom: 120px;
      right: 120px;
      width: 100px;
      height: 100px;
      border: 3px solid #c9a145;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #fff;
      z-index: 3;
    }

    .seal-inner {
      width: 80px;
      height: 80px;
      border: 2px solid #0a2351;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 12px;
      font-weight: bold;
      color: #0a2351;
      text-transform: uppercase;
      letter-spacing: 1px;
      text-align: center;
      line-height: 1.4;
      padding: 5px;
    }
  </style>
</head>
<body>
  <div class="certificate-container">
    <div class="corner-decoration top-left"></div>
    <div class="corner-decoration top-right"></div>
    <div class="corner-decoration bottom-left"></div>
    <div class="corner-decoration bottom-right"></div>

    <div class="certificate-header">
      <h1 class="certificate-title">Certificate</h1>
      <div class="certificate-subtitle">of Appreciation</div>
    </div>

    <img src="/IMG/desbord_logo.png" alt="Logo" class="logo-top-left" />

    <div class="certificate-content">
      <p><strong>NAME:</strong> <%= data.name %></p>
      <p>has successfully completed the course</p>
      <p><strong><%= data.course %></strong></p>
      <p>with distinction and exceptional dedication</p>
    </div>

    <div class="seal">
      <div class="seal-inner">Official Seal<br>RID BHARAT</div>
    </div>

    <div class="footer">
      <p>Issued by: RID BHARAT</p>
      <div class="signature">Authorized Signature</div>
      <p class="date">Date: <%= data.date ? data.date.toDateString() : 'Date not available' %></p>
    </div>
  </div>
</body>
</html>